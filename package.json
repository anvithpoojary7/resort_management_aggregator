{
  "name": "resort_management_aggregator",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && node server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.5",
    "framer-motion": "^12.20.1",
    "nodemailer": "^7.0.5",
    "react-router-dom": "^7.6.3",
    "recharts": "^3.0.2",
    "tailwind-merge": "^3.3.1"
  }
}
